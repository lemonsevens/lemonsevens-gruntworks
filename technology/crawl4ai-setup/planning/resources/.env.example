# Example .env file for crawl4ai service and PostgreSQL
# Copy this to .env and fill in your actual values

# PostgreSQL Credentials
POSTGRES_DB=mydatabase
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mysecretpassword

# Optional: Database host for crawl4ai script (use service name from docker-compose)
DB_HOST=postgres_db
DB_PORT=5432

# Optional: crawl4ai specific settings if needed via environment variables
# CRAWL4AI_SETTING_1=value1

# Optional: Webhook endpoint if needed by the script
# WEBHOOK_URL=https://your-n8n-instance/webhook/some-id 